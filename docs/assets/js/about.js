/* about.js — the credits page: chrome + scroll reveals (external so the site can
   run without inline scripts) */
document.addEventListener('DOMContentLoaded', function () {
  window.IPL.renderChrome('about.html');
  window.IPL.reveal();
});
